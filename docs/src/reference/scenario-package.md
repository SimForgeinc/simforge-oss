# Scenario package format

The normative specification of `simforge.scenario-package/v1`, as maintained
in the repository (`docs/engineering/scenario-package.md`). The JSON schema is
in `contracts/`.

{{#include ../../engineering/scenario-package.md:2:}}
