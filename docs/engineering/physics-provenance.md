# Physics provenance: measured golden references

Companion to `fixtures/physics/golden-maneuvers.v2.json` and
[physics-validation.md](physics-validation.md).

## Provenance classes

Golden maneuvers must not be validated by numbers generated by the engine they
validate. v2 of the fixture adds a per-case `provenance` field:

| provenance       | meaning                                                        | CI   |
|------------------|----------------------------------------------------------------|------|
| `measured-carla` | executed against CARLA 0.9.16 as the physics oracle            | pass |
| `published-data` | cited external figure (regulatory, standard, or compendium)    | pass |
| `engine-derived` | solver self-check, or oracle not yet achievable — no external truth claimed | **pinned** |

Rules: every entry declares a provenance; every reference row carries its own
provenance and may never be `engine-derived`; engine-derived entries are only
tolerated inside the fixture's `documentedEngineDerivedIds` pin list. The
pinned set is exactly the solver self-checks plus the maneuvers whose oracle
runs did not yet produce valid numbers (below). The engine is held inside the
measured and published bands by
`golden_maneuvers_stay_inside_the_published_parity_bands`
(`native/crates/simforge-core/src/physics/tests.rs`).

## Oracle

The oracle runner is a standalone CARLA script, not part of this repository.

* CARLA server 0.9.16 (`carlasim/carla:0.9.16`, docker), client 0.9.16, server
  with rendering disabled (`no_rendering_mode`): a physics-only oracle.
* Client note: the `carla==0.9.16` wheel segfaults in `get_world()` on an
  Ubuntu 24.04 glibc stack, so the Python client ran in a slim `python:3.10`
  sidecar container sharing the server network namespace. The server version
  string is captured in every results file as evidence.
* Vehicle: `vehicle.lincoln.mkz_2020` (mid-size sedan stand-in for the generic
  passenger-car class). Fixed step 0.02 s — deliberately equal to the
  platform's 20 ms tick. Synchronous mode, no traffic, dedicated server.
* Map selection is automated: junction-free straight chains are scanned from
  OpenDRIVE waypoints, probe-driven station-by-station (speed retention + free-fall
  detection), and the longest fully clear window is used.

Each raw results file carries `carlaServerVersion`, map, vehicle blueprint,
dt, per-maneuver records and error blocks.

## Parity table: engine vs CARLA 0.9.16

Measured with the same maneuver definitions on both sides. The engine column
was measured when the bands were set; the Rust test above keeps the current
engine inside each band.

| maneuver | metric | engine (dynamic-v1) | CARLA 0.9.16 (Lincoln MKZ) | published anchor | v2 band |
|---|---|---|---|---|---|
| straight-line acceleration | 0–100 km/h time | 8.00 s | **7.30 s** (0–60: 3.44 s), full throttle, Town01_Opt clear straight | 8.5 s mid-band of mainstream 6–10 s cluster | CARLA ±12% → [6.42, 8.18] ∩ published ±10% → [7.65, 9.35]; the engine passes both |
| straight-line braking | stopping distance 100→0 km/h | 49.53 m (ideal μ·g model) | **54.10 m** at 100.06 km/h entry, mean decel 7.14 m/s², pedal at first moment v ≥ 100 km/h | 48.2 m derived from Euro NCAP 0.90 g dry decel; FMVSS 135 ceiling 70.1 m | CARLA ±15% → [45.99, 62.22], covers both engines and the derivation, far under the ceiling |
| coastdown at 80 km/h | deceleration, pedals released | 0.256 m/s² (road load only) | 1.876 m/s² — **protocol divergence**: CARLA keeps powertrain drag engaged in drive; SAE J1263 coasts in neutral | SAE J1263 ≈ 0.25 m/s² | published-data band ±15% (the engine implements exactly this road-load model); CARLA figure recorded as informational |
| steady-state cornering | max sustained lateral g | 0.688 g (solver sweep) | *not validly measured yet* — roundabout follower produced kinematically invalid samples (rejected, see below) | family-sedan skidpad envelope 0.7–0.85 g | case stays `engine-derived`, pinned |
| double lane change (ISO 3888-1) | gate compliance / peak lateral g | harness wiring pending | runs executed at 40/55 km/h entry; gates A–B held within width, later gates hit segment obstacles; no clean pass | ISO 3888-1 course geometry (15/30/25/25/30 m, widths 1.1+B/2, B+1, offset 3.5 m) | geometry cited as `published-data`; no dynamic number banded rather than fabricated |

## Honest gaps (all pinned in `documentedEngineDerivedIds`)

1. **constant-radius-skidpad** — the Town03 roundabout lane-follower chased
   junction-interior waypoints: reported radius 0.7 m and >1 g sustained at
   24 km/h, which is kinematically impossible, so the samples were rejected.
   A proper constant-radius site (large open asphalt or a clean roundabout
   loop with closure detection) is the missing prerequisite.
2. **step-steer** — at 80 km/h and even 50 km/h, small steer steps eventually
   left the probed corridor and met roadside obstacles before settling;
   yaw-gain needs a large-area site (skidpad / airfield).
3. **double-lane-change dynamics** — course infrastructure and controller are
   complete and executed (entry speed reached, gates A/B within width at
   40 km/h); a collision-free full-course pass is
   still needed before a peak-lateral-g band can be claimed.
4. The six non-physics self-checks (numerics convergence, determinism,
   friction sweep, split-μ, collision onset timing, throughput) never had
   external references; they remain solver assertions and are now explicitly
   labeled `engine-derived`.

None of these gaps is papered over with invented numbers.

## Notable engineering findings (for future oracle users)

* `carla.Vehicle.set_target_velocity` is a *one-shot* velocity injection, but
  teleporting a moving actor (`set_transform`) can glitch PhysX — wraps that
  preserve speed must be followed by a stability check before measuring.
* Gear 0 with `manual_gear_shift` in CARLA is reverse, not neutral; there is no
  clean neutral-coast protocol, which is why the coastdown row documents a
  protocol divergence instead of forcing comparability.
* Probe-based clear-window detection (speed retention + free-fall detection
  after suspension settle) is required: nominal "straight" chains in Town04
  contain banked curves, hills and invisible blockers; Town01_Opt's grid gave
  the longest verified-clear stretch (285 m).
