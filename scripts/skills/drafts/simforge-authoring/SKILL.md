---
name: simforge-authoring
description: Author SimForge scenarios from the command line (templates, sites, instantiate, batch, catalog, locations, variation). DRAFT: kept in scripts/skills/drafts until track A lands the authoring commands; the checker then verifies it like the others.
---

# Authoring scenarios (draft)

Waiting on the Rust authoring commands (track A: template, sites, instantiate,
batch, catalog, locations, variation). Write the workflow and the generated
reference against `simforge --help` once they exist, then move this directory
to skills/simforge-authoring.
