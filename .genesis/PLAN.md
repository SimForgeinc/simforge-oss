# Implementation plan

> Generated from `.genesis/project.json`; do not edit this file.

- workflow: new-product
- phase: verify
- plan approval: Krishna Teja at 2026-09-23T06:30:12.861Z

## Tasks

### CAM-S0-BASELINE — Publish the revision-pinned Phase 0 baseline findings and executable service-side attach-path roll proof before any FR-013-dependent work; record Sensor-path texture format, transfer and bit depth; mark checklist 0.10 and 0.11 deferred to Spec 2. Rust changes are limited to tests or equivalent executable proof under renderer/service and renderer/render-core; non-test source changes are prohibited. If AC-002 is refuted, record that result, do not adopt CAM-S1-CAP004, require CAM-S1-CAP006 to classify full-mount-rotation as approximated or unsupported with the finding as its reason, and reopen the approved plan to add a separately approved Spec 1 renderer/service fix task.

- state/risk: done / medium
- requirements: FR-001, FR-002, FR-003, FR-004, FR-005, FR-006, FR-007, FR-008, FR-009, FR-010, FR-011, AC-001, AC-002, AC-003, NFR-001, NFR-002, NFR-007
- scope: docs/engineering/camera-baseline-findings.md, renderer/service, renderer/render-core
- gates: baseline-findings: node -e "const fs=require('fs');const p='docs/engineering/camera-baseline-findings.md';const s=fs.readFileSync(p,'utf8');for(const x of ['998be4df','c0a0b08','texture','transfer','bit depth','0.10','Spec 2','0.11'])if(!s.includes(x))process.exit(1)", service-roll: cargo test --manifest-path renderer/Cargo.toml -p service, independent-review: pass
- next: Write source-cited findings and add only a Rust test or equivalent executable proof; do not change non-test Rust source. Run both gates and obtain independent review. If AC-002 is refuted, record the contingency and stop CAP004 adoption; otherwise checkpoint before handoff.

### CAM-S1-CAP002 — After CAM-S0-BASELINE is verified and this task is active, create branch camera/phase-1-config-capability from 998be4df, then adopt camera profile representation as one unsquashed commit naming CAM-01-CAP-002, Genesis Spec 1 and checkpoint 1; preserve generic-uncalibrated identity plus outputStage and encoding values sourced from CAM-S0-BASELINE.

- state/risk: done / medium
- requirements: FR-012, FR-020, AC-004, NFR-001, NFR-002, NFR-005, NFR-007
- scope: packages/scenario/src/schema/v2/sensors.ts, packages/scenario/src/__tests__/sensors.test.ts
- gates: profile-contract: pnpm --filter @simforge-oss/scenario exec vitest run src/__tests__/sensors.test.ts, independent-review: pass
- next: Create camera/phase-1-config-capability from 998be4df without altering the preserved diff, reconcile only CAP-002 source/tests, run the gate, obtain independent review, checkpoint, then create its capability commit.

### CAM-S1-CAP008 — Adopt authoring-to-render-spec camera profile propagation as one unsquashed commit naming CAM-01-CAP-008, Genesis Spec 1 and checkpoint 1.

- state/risk: done / medium
- requirements: FR-012, FR-016, AC-004, NFR-001, NFR-002, NFR-005
- scope: packages/scenario/src/render-spec.ts, packages/scenario/src/render-spec-builders.ts, packages/scenario/src/__tests__/sensors.test.ts
- gates: render-spec-propagation: pnpm --filter @simforge-oss/scenario exec vitest run src/__tests__/sensors.test.ts, independent-review: pass
- next: Isolate CAP-008 changes, run the gate, obtain independent review, checkpoint, then create its capability commit.

### CAM-S1-CAP009 — Adopt current checkpoint-1 profile/effective-config native propagation and evidence as one unsquashed commit naming CAM-01-CAP-009, Genesis Spec 1 and checkpoint 1 without claiming mandatory v2 validation.

- state/risk: done / medium
- requirements: FR-016, AC-011, NFR-001, NFR-002, NFR-003, NFR-005
- scope: packages/render/src/capabilities.ts, packages/render/src/native/engine.ts, packages/render/src/native/engine.test.ts, packages/render/src/native/engine.e2e.test.ts, packages/render/src/native/camera-schedule.test.ts, packages/render/src/native/evidence.ts, packages/render/src/native/evidence.test.ts
- gates: checkpoint-evidence: pnpm --filter @simforge-oss/render exec vitest run src/native/engine.test.ts src/native/evidence.test.ts, independent-review: pass
- next: Isolate CAP-009 checkpoint evidence only, run the gate, obtain independent review, checkpoint, then create its capability commit.

### CAM-S1-POST-SERVICE-ROLL — After checkpoint 1, create the stacked Phase 1 completion branch from the tip of camera/phase-1-config-capability, apply the parked TypeScript roll hunks from .genesis/patches/CAM-S1-POST-SERVICE-ROLL.patch, and implement the matching orientation-preserving retained service/render-core camera pose. Prove nonzero authored roll reaches the service with the reflected sign and changes camera orientation/horizon direction, then land the parked TypeScript hunks and service fix together as one unsquashed commit naming CAM-01-CAP-004, Genesis Spec 1 and checkpoint 2. This work must not enter the checkpoint-1 PR.

- state/risk: queued / medium
- requirements: FR-013, AC-002, AC-011, NFR-001, NFR-002, NFR-005, NFR-007
- scope: renderer/render-core/src/engine.rs, renderer/service/src/server.rs, renderer/service/src/proto.rs, .genesis/patches/CAM-S1-POST-SERVICE-ROLL.patch, packages/render/src/native/camera-schedule.ts, packages/render/src/native/camera-schedule.test.ts
- gates: service-orientation: cargo test --manifest-path renderer/Cargo.toml -p service, rotation-propagation: pnpm --filter @simforge-oss/render test -- src/native/camera-schedule.test.ts, independent-review: pending
- next: After checkpoint 1 and separate human approval, create the stacked completion branch from the exact checkpoint branch tip, apply the parked patch, implement the smallest matching orientation-preserving service/render-core pose seam, run both gates, obtain independent review, checkpoint, and commit the TypeScript and Rust service fix together as CAM-01-CAP-004 checkpoint 2.

### CAM-S1-CAP006 — Adopt the native camera capability declaration as one unsquashed commit naming CAM-01-CAP-006, Genesis Spec 1 and checkpoint 1. Declare full-mount-rotation approximated with the exact reason roll dropped by SceneApp::set_pose (KNOWLEDGE-9f3d54a8), record that approximation in effective camera configuration and native manifest evidence, and emit the same reason as a render warning. Do not hard-reject nonzero authored roll at checkpoint 1 so existing dash cameras remain compatible under FR-020; never claim full mount rotation supported.

- state/risk: done / medium
- requirements: FR-014, AC-005, AC-011, NFR-001, NFR-002, NFR-003, NFR-005
- scope: packages/render/src/capabilities.ts, packages/render/src/native/engine.ts, packages/render/src/native/engine.test.ts, packages/render/src/native/evidence.ts, packages/render/src/native/evidence.test.ts
- gates: capability-declaration: pnpm --filter @simforge-oss/render exec vitest run src/native/engine.test.ts src/native/evidence.test.ts, independent-review: pass
- next: Isolate CAP-006 declaration/evidence changes, record full-mount-rotation as approximated with reason roll dropped by SceneApp::set_pose (KNOWLEDGE-9f3d54a8), preserve nonzero-roll acceptance, run the gate, obtain independent review, checkpoint, then create its capability commit.

### CAM-S1-CAP007 — Adopt structured rejection for rolling shutter, PTC noise and raw output as one unsquashed commit naming CAM-01-CAP-007, Genesis Spec 1 and checkpoint 1; explicitly leave projection-family rejection for the post-checkpoint task.

- state/risk: done / medium
- requirements: FR-014, AC-005, AC-011, NFR-001, NFR-002, NFR-003, NFR-005
- scope: packages/render/src/capabilities.ts, packages/render/src/native/engine.ts, packages/render/src/native/engine.test.ts
- gates: structured-rejection: pnpm --filter @simforge-oss/render exec vitest run src/native/engine.test.ts, independent-review: pass
- next: Isolate only preserved CAP-007 rejection changes, run the gate, obtain independent review, checkpoint, then create its capability commit.

### CAM-S1-CAP020 — Adopt dataset Sensor-profile selection with autoMeter=false as one unsquashed commit naming CAM-03-CAP-020, Genesis Spec 1 and checkpoint 1 while preserving cinematic review behavior.

- state/risk: done / medium
- requirements: FR-015, AC-009, AC-011, NFR-001, NFR-002, NFR-004, NFR-005, NFR-007
- scope: packages/render/src/native/engine.ts, packages/render/src/native/engine.test.ts, packages/render/src/native/evidence.ts, packages/render/src/native/evidence.test.ts
- gates: sensor-mode: pnpm --filter @simforge-oss/render exec vitest run src/native/engine.test.ts src/native/evidence.test.ts, independent-review: pass
- next: Isolate CAP-020 changes, run the gate, obtain independent review, checkpoint, then create its capability commit.

### CAM-S1-SCHEMA — Regenerate scenario-template.v2 schema deterministically and commit generated schemas alone in a separate unsquashed chore commit after all checkpoint-1 capability commits.

- state/risk: done / low
- requirements: FR-020, AC-010, NFR-001, NFR-005, NFR-006
- scope: packages/scenario/schema/scenario-template.v2.schema.json
- gates: schema-determinism: node -e "const{execFileSync}=require('child_process'),{readFileSync}=require('fs'),{createHash}=require('crypto');const p='packages/scenario/schema/scenario-template.v2.schema.json';const h=()=>createHash('sha256').update(readFileSync(p)).digest('hex');const before=h();execFileSync('pnpm',['--filter','@simforge-oss/scenario','schema'],{stdio:'inherit'});if(before!==h())process.exit(1)"
- next: Regenerate once, prove byte stability, checkpoint, and create a generated-schema-only chore commit.

### CAM-S1-CHECKPOINT-PR — Preserve the capability commits unsquashed and open (never ready or merge) the draft PR titled Camera fidelity — Phase 1 slice (config & capability layer) [Genesis Spec 1, checkpoint 1]. Its body must contain the §12.1 evidence table, partial gate status, all eight unchecked §12.3 review items, non-claims, explicit GPU engine.e2e/golden-gate non-runs, the Yale fixture skip, and cargo fmt --check blocked by pre-existing drift with no unrelated reformatting. Exclude the parked roll patch and all post-checkpoint follow-up implementation.

- state/risk: rejected / medium
- requirements: AC-010, AC-011, NFR-001, NFR-002, NFR-003
- scope: SPEC.md
- gates: checkpoint-commits: node -e "const{execFileSync}=require('child_process');const s=execFileSync('git',['log','--format=%s','998be4df40575b6c450067824f2c6aac50cd2b12..HEAD'],{encoding:'utf8'});for(const id of ['CAM-01-CAP-002','CAM-01-CAP-008','CAM-01-CAP-009','CAM-01-CAP-006','CAM-01-CAP-007','CAM-03-CAP-020'])if(s.split(id).length-1!==1)process.exit(1);if(s.includes('CAM-01-CAP-004'))process.exit(1);if(!/generated schema/i.test(s))process.exit(1)", draft-pr-contract: node -e "const{execFileSync}=require('child_process');const p=JSON.parse(execFileSync('gh',['pr','view','--json','isDraft,title,body'],{encoding:'utf8'}));const title='Camera fidelity — Phase 1 slice (config & capability layer) [Genesis Spec 1, checkpoint 1]';const required=['§12.1','partial','§12.3','GPU','engine.e2e.test.ts','golden gate','no device fidelity','no rolling-shutter implementation','no noise model','Yale fixture','skipped','cargo fmt --check','blocked, pre-existing','no unrelated reformatting'];if(!p.isDraft||p.title!==title||required.some(x=>!p.body.includes(x)))process.exit(1);if((p.body.match(/- \[ \]/g)||[]).length<8)process.exit(1)", independent-review: pass
- next: After all dependencies are verified and checkpointed, retain the six capability commits plus the generated-schema commit on the branch created by CAM-S1-CAP002, prove CAM-01-CAP-004 is absent, push, open the exact draft PR, run both gates, obtain independent review, and stop without marking ready or merging.

### CAM-S1-POST-PROJECTION — On the stacked Phase 1 completion branch, after manifest v2, CAM-01-CAP-005 and the geometry fixture, add unsupported projection-family rejection with stable machine-readable reasons. This is new work and must not enter the checkpoint-1 PR.

- state/risk: queued / medium
- requirements: FR-014, AC-005, NFR-001, NFR-002, NFR-003, NFR-005
- scope: packages/render/src/native/engine.ts, packages/render/src/native/engine.test.ts
- gates: projection-rejection: pnpm --filter @simforge-oss/render test -- src/native/engine.test.ts, independent-review: pending
- next: Do not start in the checkpoint delivery run. After the gate-critical follow-up tasks pass on the stacked completion branch, implement only projection-family rejection, then run/review/checkpoint.

### CAM-S1-POST-MANIFEST-V2 — On the stacked Phase 1 completion branch created by CAM-S1-POST-SERVICE-ROLL, emit mandatory camera-profile evidence under native render-manifest v2, conditionally move diagnostics to v2, enforce fields in evidence.ts, and parse v1 read-only as camera-profile-evidence: absent (pre-v2); exclude this from the checkpoint-1 PR.

- state/risk: queued / medium
- requirements: FR-016, FR-017, AC-008, NFR-001, NFR-002, NFR-003, NFR-005
- scope: packages/render/src/native/engine.ts, packages/render/src/native/engine.test.ts, packages/render/src/native/evidence.ts, packages/render/src/native/evidence.test.ts
- gates: manifest-v2: pnpm --filter @simforge-oss/render test -- src/native/engine.test.ts src/native/evidence.test.ts, independent-review: pending
- next: After the service-roll task passes on the stacked completion branch, implement v2 enforcement and legacy classification with negative tests, then run/review/checkpoint without updating checkpoint PR 1.

### CAM-S1-POST-CAP005 — On the stacked Phase 1 completion branch, implement CAM-01-CAP-005 actual-versus-reported calibration isolation with consumer non-leakage; exclude it from the checkpoint-1 PR.

- state/risk: queued / medium
- requirements: FR-018, AC-007, NFR-001, NFR-002, NFR-003, NFR-005, NFR-007
- scope: packages/scenario/src/schema/v2/sensors.ts, packages/scenario/src/schema/v2/sensor-rigs.ts, packages/scenario/src/render-spec.ts, packages/scenario/src/render-spec-builders.ts, packages/scenario/src/__tests__/sensors.test.ts, packages/render/src/native/engine.ts, packages/render/src/native/engine.test.ts
- gates: calibration-isolation: pnpm --filter @simforge-oss/scenario test -- src/__tests__/sensors.test.ts && pnpm --filter @simforge-oss/render test -- src/native/engine.test.ts, independent-review: pending
- next: Do not start until prior follow-up tasks pass; implement the smallest separated calibration views and leakage tests, then run/review/checkpoint.

### CAM-S1-POST-GEOMETRY — On the stacked Phase 1 completion branch, add the nonzero yaw/pitch/roll plus off-centre principal-point fixture that proves selected-path projection and exported calibration agreement; exclude it from the checkpoint-1 PR.

- state/risk: queued / medium
- requirements: FR-013, FR-019, AC-006, NFR-001, NFR-002, NFR-005
- scope: packages/render/src/native/camera-schedule.test.ts, packages/render/src/native/engine.test.ts
- gates: geometric-fixture: pnpm --filter @simforge-oss/render test -- src/native/camera-schedule.test.ts src/native/engine.test.ts, independent-review: pending
- next: Do not start until actual/reported calibration is available; add one analytic fixture, then run/review/checkpoint.

### CAM-S1-FINAL-VERIFY — Verify completed Spec 1 against the current source hash without promoting checkpoint-1 evidence, then open a second stacked draft PR against camera/phase-1-config-capability titled Camera fidelity — Phase 1 completion [Genesis Spec 1, checkpoint 2]. Run workspace typecheck, all scenario tests, focused native camera tests, deterministic schema generation and diff hygiene; report GPU engine.e2e and golden-gate status truthfully.

- state/risk: queued / medium
- requirements: AC-004, AC-005, AC-006, AC-007, AC-008, AC-009, AC-010, AC-011, NFR-001, NFR-002, NFR-003, NFR-004, NFR-005, NFR-006, NFR-007
- scope: packages/scenario, packages/render
- gates: workspace-typecheck: pnpm typecheck, scenario-suite: pnpm --filter @simforge-oss/scenario test, native-camera-suite: pnpm --filter @simforge-oss/render test -- src/native/camera-schedule.test.ts src/native/engine.test.ts src/native/evidence.test.ts, diff-hygiene: git diff --check, stacked-pr-contract: node -e "const{execFileSync}=require('child_process');const p=JSON.parse(execFileSync('gh',['pr','view','--json','isDraft,title,baseRefName'],{encoding:'utf8'}));if(!p.isDraft||p.title!=='Camera fidelity — Phase 1 completion [Genesis Spec 1, checkpoint 2]'||p.baseRefName!=='camera/phase-1-config-capability')process.exit(1)", independent-review: pending
- next: Run all mandatory gates against one source hash, obtain independent review, checkpoint, push the stacked completion branch, open the exact second draft PR against camera/phase-1-config-capability, verify its contract, and report skipped or unrun GPU/golden checks without promotion.

### CAM-S1-COMPAT — Repair only the checkpoint-discovered generic camera-profile integration fixtures, pass full workspace typecheck and the complete scenario suite, independently review, checkpoint, and create one narrow compatibility commit without a capability ID.

- state/risk: done / medium
- requirements: AC-010
- scope: packages/studio-host/src/node/collision-draft-lowering.ts, packages/scenario/src/__tests__/render-spec.test.ts
- gates: integration-compatibility: pnpm typecheck && pnpm --filter @simforge-oss/scenario test, independent-review: pass
- next: Run the task pre-flight.

### CAM-S1-CHECKPOINT-COMPAT — Key camera-profile capability enforcement on request explicitness. Carry profileSource: default | authored through render specs. Schema-default profiles contribute preferred capabilities and, on engines without camera.* declarations, record camera-profile: not-declared-by-engine (approximated) with a warning while rendering unchanged. Explicitly authored profiles/features contribute required capabilities and fail preflight with structured missing-capability reasons on every unsupported engine, including browser and CARLA. Add regressions for default browser/CARLA acceptance, explicit CARLA linear-RGB rejection, and unchanged native behavior; land one narrow non-capability checkpoint commit.

- state/risk: done / medium
- requirements: AC-010, FR-020, NFR-002, NFR-003, NFR-005
- scope: packages/scenario/src/schema/v2/sensors.ts, packages/scenario/src/schema/v2/sensor-rigs.ts, packages/scenario/src/render-spec.ts, packages/scenario/src/render-spec-builders.ts, packages/scenario/src/__tests__/sensors.test.ts, packages/scenario/src/__tests__/render-spec.test.ts, packages/scenario/schema/scenario-template.v2.schema.json, packages/studio-host/src/node/collision-draft-lowering.ts, packages/render/src/artifacts.ts, packages/render/src/capabilities.ts, packages/render/src/capabilities.test.ts, packages/render/src/camera-test-fixture.ts, packages/render/src/builtin-engines.ts, packages/render/src/native/camera-schedule.test.ts, packages/render/src/native/engine.ts, packages/render/src/native/engine.e2e.test.ts, packages/render/src/native/engine.test.ts, packages/render/src/native/evidence.test.ts, packages/render/src/web/engine.ts, packages/render/src/web/engine.test.ts, packages/render/src/builtin-engines.test.ts, packages/render/src/worker-control.ts, packages/render/src/worker-control.test.ts, services/render-worker/src/worker.ts, services/render-worker/src/transport.test.ts, studio/app/lib/scenario/render-wire-contracts.ts, studio/app/lib/scenario/render-worker-control-store.ts
- gates: explicit-profile-compatibility: pnpm --filter @simforge-oss/scenario exec vitest run src/__tests__/sensors.test.ts src/__tests__/render-spec.test.ts && pnpm --filter @simforge-oss/render exec vitest run src/capabilities.test.ts src/native/engine.test.ts src/web/engine.test.ts src/builtin-engines.test.ts src/worker-control.test.ts && pnpm --filter @simforge-oss/render-worker test && pnpm --filter @simforge-oss/render-worker exec tsc --noEmit && pnpm typecheck, independent-review: pass
- next: Carry profileSource through render specs, key required versus preferred capability intent on it, record the default-profile approximation and warning, add the four focused regressions, run the gate, obtain independent review, checkpoint, create one non-capability commit, push PR #22, and stop.

### CAM-S1-CHECKPOINT-COMPAT-REVIEW — Correct the checkpoint-review compatibility defects without claiming a capability: accept the authoritative native camera capability declaration in Studio worker registration, reject non-generic profiles mislabeled as schema defaults at sensor and render-spec boundaries, and keep mixed default/authored camera capabilities disjoint between required and preferred. Add focused regressions, preserve draft PR scope, and land one non-capability checkpoint-1 correction commit.

- state/risk: rejected / medium
- requirements: AC-010, FR-020, NFR-002, NFR-003, NFR-005
- scope: studio/app/lib/scenario/render-wire-contracts.ts, studio/app/lib/scenario/render-wire-contracts.test.ts, packages/scenario/src/schema/v2/sensors.ts, packages/scenario/src/render-spec.ts, packages/scenario/src/render-spec-builders.ts, packages/scenario/src/__tests__/sensors.test.ts, packages/scenario/src/__tests__/render-spec.test.ts, packages/studio-host/src/node/collision-draft-lowering.ts
- gates: checkpoint-review-compatibility: pnpm --filter @simforge-oss/scenario exec vitest run src/__tests__/sensors.test.ts src/__tests__/render-spec.test.ts && pnpm --filter @simforge-oss/studio exec tsx --test app/lib/scenario/render-wire-contracts.test.ts && pnpm typecheck, independent-review: pending
- next: Run the task pre-flight.

### CAM-S1-CHECKPOINT-COMPAT-REVIEW-FINAL — Land the approved checkpoint-review correction as one non-capability commit: Studio accepts authoritative native camera capabilities and approximations with native-declaration and checkpoint-manifest fixtures; profile provenance is derived from authored property presence and schema defaults serialize without a profile property; required and preferred camera capabilities are disjoint across mixed rigs through one shared merge; browser and CARLA default compatibility remains intact.

- state/risk: done / medium
- requirements: AC-010, FR-020, NFR-002, NFR-003, NFR-005
- scope: studio/app/lib/scenario/render-wire-contracts.ts, studio/app/lib/scenario/render-wire-contracts.test.ts, packages/scenario/src/schema/v2/sensors.ts, packages/scenario/src/schema/v2/sensor-rigs.ts, packages/scenario/src/render-spec.ts, packages/scenario/src/render-spec-builders.ts, packages/scenario/src/__tests__/sensors.test.ts, packages/studio-host/src/node/collision-draft-lowering.ts, packages/render/src/camera-test-fixture.ts
- gates: checkpoint-review-final: pnpm --filter @simforge-oss/scenario exec vitest run src/__tests__/sensors.test.ts src/__tests__/render-spec.test.ts && pnpm --filter @simforge-oss/studio exec tsx --test app/lib/scenario/render-wire-contracts.test.ts && pnpm --filter @simforge-oss/render test && pnpm typecheck && git diff --check, independent-review: pass
- next: Run the task pre-flight.

### CAM-S1-CHECKPOINT-PR-FINAL — Verify without modifying PR #22 that checkpoint 1 remains draft at exact head 3f4a4a99ecf7bf191ebb2dd42438572b988afd52, preserves its exact title and partial claims, excludes CAM-01-CAP-004, and passes the Studio native worker capability plus checkpoint-manifest schema fixture required by final checkpoint review.

- state/risk: done / medium
- requirements: AC-010, AC-011, NFR-001, NFR-002, NFR-003
- scope: studio/app/lib/scenario/render-wire-contracts.ts, studio/app/lib/scenario/render-wire-contracts.test.ts
- gates: draft-checkpoint-final: node -e "const{execFileSync}=require(\"child_process\");const p=JSON.parse(execFileSync(\"gh\",[\"pr\",\"view\",\"22\",\"--json\",\"isDraft,state,title,headRefOid\"],{encoding:\"utf8\"}));if(!p.isDraft||p.state!==\"OPEN\"||p.headRefOid!==\"3f4a4a99ecf7bf191ebb2dd42438572b988afd52\"||p.title!==\"Camera fidelity — Phase 1 slice (config & capability layer) [Genesis Spec 1, checkpoint 1]\")process.exit(1)" && pnpm --filter @simforge-oss/studio exec tsx --test app/lib/scenario/render-wire-contracts.test.ts, independent-review: pass
- next: Run the task pre-flight.

### CAM-S1-POST-SERVICE-ROLL-FINAL — From exact checkpoint-1 tip 3f4a4a99, implement and prove CAM-01-CAP-004 only: preserve the full mount quaternion through render-core/service, apply the parked TypeScript roll conversion, prove +10 degree authored roll reaches attach.rollDeg with the reflected sign, make the Spec 0 AC-002 service test pass, add a SIMFORGE_NATIVE_E2E=1 rendered-horizon assertion, and move full-mount-rotation from approximated to supported with integrated evidence on checkpoint 2. Land all source changes as one unsquashed non-manifest-v2 commit and stop.

- state/risk: done / medium
- requirements: FR-013, AC-002, AC-011, NFR-001, NFR-002, NFR-005, NFR-007
- scope: renderer/render-core/src/engine.rs, renderer/service/src/server.rs, renderer/service/src/proto.rs, .genesis/patches/CAM-S1-POST-SERVICE-ROLL.patch, packages/render/src/native/camera-schedule.ts, packages/render/src/native/camera-schedule.test.ts, packages/render/src/native/engine.e2e.test.ts, packages/render/src/capabilities.ts, packages/render/src/native/engine.ts, packages/render/src/native/engine.test.ts, packages/render/src/native/evidence.test.ts
- gates: service-orientation: cargo test --manifest-path renderer/Cargo.toml -p service, rotation-propagation: pnpm --filter @simforge-oss/render exec vitest run src/native/camera-schedule.test.ts src/native/engine.test.ts src/native/evidence.test.ts, horizon-contract: node -e "const s=require(\"fs\").readFileSync(\"packages/render/src/native/engine.e2e.test.ts\",\"utf8\");for(const x of [\"SIMFORGE_NATIVE_E2E\",\"horizon\",\"roll\"])if(!s.includes(x))process.exit(1)", independent-review: pass
- next: Run the task pre-flight.

### CAM-S1-POST-SERVICE-ROLL-EVIDENCE — Correct checkpoint-2 CAM-01-CAP-004 evidence before manifest v2: declare full-mount-rotation supported at evidenceTier integrated while rendered proof remains pending; make the SIMFORGE_NATIVE_E2E=1 horizon assertion prove authored positive roll about +X rotates the image counter-clockwise; verify and commit the code correction; separately commit camera baseline findings and repository Genesis/SPEC state after proving no absolute local paths; push camera/phase-1-completion without opening a PR.

- state/risk: rejected / medium
- requirements: FR-013, AC-002, AC-011, NFR-001, NFR-002
- scope: packages/render/src/capabilities.ts, packages/render/src/native/engine.ts, packages/render/src/native/engine.test.ts, packages/render/src/native/engine.e2e.test.ts, docs/engineering/camera-baseline-findings.md, SPEC.md, .genesis
- gates: rotation-evidence: pnpm --filter @simforge-oss/render exec vitest run src/native/engine.test.ts, horizon-direction: node -e "const s=require('fs').readFileSync('packages/render/src/native/engine.e2e.test.ts','utf8');for(const x of ['SIMFORGE_NATIVE_E2E','counter-clockwise','alignedHorizon'])if(!s.includes(x))process.exit(1)", no-absolute-local-paths: node -e "const{execFileSync}=require('child_process');const s=execFileSync('grep',['-R','-n','-E','/Users/|/home/|[A-Z]:\\\\Users\\','SPEC.md','.genesis'],{encoding:'utf8'});if(s.trim())process.exit(1)", diff-check: git diff --check, independent-review: pending
- next: Run the task pre-flight.

### CAM-S1-POST-SERVICE-ROLL-EVIDENCE-FINAL — Correct checkpoint-2 CAM-01-CAP-004 evidence before manifest v2: declare full-mount-rotation supported at evidenceTier integrated while rendered proof remains pending; make the SIMFORGE_NATIVE_E2E=1 horizon assertion prove authored positive roll about +X rotates the image counter-clockwise; verify and commit the code correction; separately commit camera baseline findings and repository Genesis/SPEC state after proving intended tracked files contain no absolute local paths; push camera/phase-1-completion without opening a PR.

- state/risk: rejected / medium
- requirements: FR-013, AC-002, AC-011, NFR-001, NFR-002
- scope: packages/render/src/capabilities.ts, packages/render/src/native/engine.ts, packages/render/src/native/engine.test.ts, packages/render/src/native/engine.e2e.test.ts, docs/engineering/camera-baseline-findings.md, SPEC.md, .genesis
- gates: rotation-evidence: pnpm --filter @simforge-oss/render exec vitest run src/native/engine.test.ts, horizon-direction: node -e "const s=require('fs').readFileSync('packages/render/src/native/engine.e2e.test.ts','utf8');for(const x of ['SIMFORGE_NATIVE_E2E','counter-clockwise','alignedHorizon'])if(!s.includes(x))process.exit(1)", no-absolute-local-paths: node -e "const fs=require('fs'),root=process.cwd(),files=['SPEC.md','.genesis/KICKOFF.md','.genesis/PLAN.md','.genesis/project.json','.genesis/patches/CAM-S1-POST-SERVICE-ROLL.patch'];for(const f of files){const s=fs.readFileSync(f,'utf8');if(s.includes(root)||s.includes('file://'+root))throw Error(f)}", diff-check: git diff --check, independent-review: pending
- next: Run the task pre-flight.

### CAM-S1-POST-SERVICE-ROLL-EVIDENCE-V2 — Correct checkpoint-2 CAM-01-CAP-004 evidence before manifest v2: declare full-mount-rotation supported at evidenceTier integrated across native and Studio capability contracts while rendered proof remains pending; make the SIMFORGE_NATIVE_E2E=1 horizon assertion prove authored positive roll about +X rotates the image counter-clockwise; verify and commit the code correction; separately commit camera baseline findings and repository Genesis/SPEC state after proving intended tracked files contain no absolute local paths; push camera/phase-1-completion without opening a PR.

- state/risk: rejected / medium
- requirements: FR-013, AC-002, AC-011, NFR-001, NFR-002
- scope: packages/render/src/capabilities.ts, packages/render/src/native/engine.ts, packages/render/src/native/engine.test.ts, packages/render/src/native/engine.e2e.test.ts, studio/app/lib/scenario/render-wire-contracts.ts, studio/app/lib/scenario/render-wire-contracts.test.ts, docs/engineering/camera-baseline-findings.md, SPEC.md, .genesis
- gates: rotation-evidence: pnpm --filter @simforge-oss/render exec vitest run src/native/engine.test.ts, studio-capability-contract: node --import tsx --test studio/app/lib/scenario/render-wire-contracts.test.ts, horizon-direction: node -e "const s=require('fs').readFileSync('packages/render/src/native/engine.e2e.test.ts','utf8');for(const x of ['SIMFORGE_NATIVE_E2E','counter-clockwise','alignedHorizon'])if(!s.includes(x))process.exit(1)", no-absolute-local-paths: node -e "const fs=require('fs'),root=process.cwd(),files=['SPEC.md','.genesis/KICKOFF.md','.genesis/PLAN.md','.genesis/project.json','.genesis/patches/CAM-S1-POST-SERVICE-ROLL.patch'];for(const f of files){const s=fs.readFileSync(f,'utf8');if(s.includes(root)||s.includes('file://'+root))throw Error(f)}", diff-check: git diff --check, independent-review: pending
- next: Run the task pre-flight.

### CAM-S1-POST-SERVICE-ROLL-EVIDENCE-V3 — Correct checkpoint-2 CAM-01-CAP-004 evidence before manifest v2: declare full-mount-rotation supported at evidenceTier integrated across native and Studio capability contracts while rendered proof remains pending; make the SIMFORGE_NATIVE_E2E=1 horizon assertion prove authored positive roll about +X rotates the image counter-clockwise; verify and commit the code correction; separately commit camera baseline findings and repository Genesis/SPEC state after proving intended tracked files contain no absolute local paths; push camera/phase-1-completion without opening a PR.

- state/risk: done / medium
- requirements: FR-013, AC-002, AC-011, NFR-001, NFR-002
- scope: packages/render/src/capabilities.ts, packages/render/src/native/engine.ts, packages/render/src/native/engine.test.ts, packages/render/src/native/engine.e2e.test.ts, studio/app/lib/scenario/render-wire-contracts.ts, studio/app/lib/scenario/render-wire-contracts.test.ts, docs/engineering/camera-baseline-findings.md, SPEC.md, .genesis
- gates: rotation-evidence: pnpm --filter @simforge-oss/render exec vitest run src/native/engine.test.ts, studio-capability-contract: pnpm --filter @simforge-oss/studio exec tsx --test app/lib/scenario/render-wire-contracts.test.ts, horizon-direction: node -e "const s=require('fs').readFileSync('packages/render/src/native/engine.e2e.test.ts','utf8');for(const x of ['SIMFORGE_NATIVE_E2E','counter-clockwise','alignedHorizon'])if(!s.includes(x))process.exit(1)", no-absolute-local-paths: node -e "const fs=require('fs'),root=process.cwd(),files=['SPEC.md','.genesis/KICKOFF.md','.genesis/PLAN.md','.genesis/project.json','.genesis/patches/CAM-S1-POST-SERVICE-ROLL.patch'];for(const f of files){const s=fs.readFileSync(f,'utf8');if(s.includes(root)||s.includes('file://'+root))throw Error(f)}", diff-check: git diff --check, independent-review: pass
- next: Run the task pre-flight.
