# Third-party notices

The `simforge` binary is licensed under Apache-2.0 (see `LICENSE` and `NOTICE`).
It statically links the Rust crates listed below. Each is distributed under the
licence shown, whose full text follows.

This file is generated by `scripts/release/notices.sh` (cargo-about) from the
shipped dependency graph. Do not edit it by hand.

Not linked and not bundled: ffmpeg and x264 (GPL), which `simforge render`
runs as a separate system program when present; the Vulkan loader and drivers
(`libvulkan.so.1`, Metal, DX12), which are loaded at runtime from the system.
Asset licences (the CC-BY-4.0 actor models) travel with the asset closures as
`ATTRIBUTION.json`; see the "Asset attributions" section at the end of this file.

## Overview

{{#each overview}}
- {{{name}}} ({{count}} crate{{#if (gt count 1)}}s{{/if}})
{{/each}}

## Licences

{{#each licenses}}
### {{{name}}}

Used by:
{{#each used_by}}
- [{{crate.name}} {{crate.version}}]({{#if crate.repository}}{{crate.repository}}{{else}}https://crates.io/crates/{{crate.name}}{{/if}})
{{/each}}

```text
{{{text}}}
```

{{/each}}
