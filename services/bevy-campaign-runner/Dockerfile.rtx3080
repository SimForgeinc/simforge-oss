FROM ubuntu:24.04

ARG SOURCE_REVISION
LABEL org.opencontainers.image.revision="${SOURCE_REVISION}" \
      org.opencontainers.image.source="https://github.com/SimForgeinc/simforge-oss" \
      org.opencontainers.image.title="simforge-oss standalone Bevy campaign runner" \
      ai.simforge.hardware-profile="rtx3080-10gb-v1"

ENV DEBIAN_FRONTEND=noninteractive \
    NVIDIA_VISIBLE_DEVICES=all \
    NVIDIA_DRIVER_CAPABILITIES=compute,graphics,utility
RUN apt-get update \
 && apt-get install -y --no-install-recommends ca-certificates ffmpeg libasound2t64 libudev1 libvulkan1 libx11-6 libxkbcommon0 python3 python3-numpy vulkan-tools \
 && rm -rf /var/lib/apt/lists/*

COPY renderer/target/release/simforge-render /usr/local/bin/simforge-render
COPY renderer/service/python/simforge_render /opt/simforge/simforge_render
COPY scripts/bevy-campaign-parity.py /usr/local/bin/bevy-campaign-parity

RUN chmod 0755 /usr/local/bin/simforge-render /usr/local/bin/bevy-campaign-parity
WORKDIR /work
ENTRYPOINT ["/usr/local/bin/bevy-campaign-parity"]
