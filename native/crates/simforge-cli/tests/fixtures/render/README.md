Golden outputs of the TypeScript native engine's scene assembly, read by `tests/render_scene.rs`. They were generated once on 2026-09-24 by running `packages/render/src/native/{timeline-lowering,camera-schedule,lighting,luminaires}.ts` and `packages/render/src/schedule.ts` under tsx, with the WASM timeline runtime built from the same sources, on the inputs named in each file. Regenerate them only when the TypeScript reference changes on purpose.
