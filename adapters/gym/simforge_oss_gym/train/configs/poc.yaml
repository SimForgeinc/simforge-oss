schema: simforge.train/v1
recipe: ppo-teacher
# Explicitly the recorded overnight episodes; provenance records their SHA-256.
# For admitted W2 splits, replace these with materialization.episodes.path from
# qualification/training-splits/poc-v1/{train,val}.split.json.
train: ~/simforge-assets/runs/drive/training/ppo-visible-20260922/scenarios/train.episodes.json
val: ~/simforge-assets/runs/drive/training/ppo-visible-20260922/scenarios/val.episodes.json
out: ~/simforge-assets/runs/drive/training/ppo-teacher
options:
  device: cpu
  seed: 42
  num_envs: 32
  threads: 8
  torch_threads: 4
  hidden: 1024
  rollout_steps: 64
  minibatch: 1024
  epochs: 4
  checkpoint_every: 20
  max_env_steps: 30000000
  hours: 6
