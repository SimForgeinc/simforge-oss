/**
 * The utility-route frame. It is the `PageShell` primitive from studio-ui;
 * this name stays for the routes that already import it.
 */
export { PageShell as AppStage, type PageShellProps as AppStageProps } from "@simforge-oss/studio-ui/components/ui/page-shell";
