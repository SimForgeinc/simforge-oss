import type { Metadata } from "next";

/**
 * Metadata a host layers over the root layout's (a slot: see
 * host-slots.json), e.g. its own icons and social images. The local host adds
 * none.
 */
export const hostMetadata: Metadata = {};
