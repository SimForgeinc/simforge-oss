import * as stylex from "@stylexjs/stylex";
import { colors, radii, space, text } from "@simforge-oss/studio-ui/stylex/tokens.stylex";

export const styles = stylex.create({
  header: {
    display: "flex",
    alignItems: "flex-start",
    justifyContent: "space-between",
    gap: space.lg,
    flexWrap: "wrap",
  },
  heading: {
    display: "flex",
    flexDirection: "column",
    gap: space.xs,
  },
  title: {
    margin: 0,
    fontFamily: text.fontDisplay,
    fontSize: text.sizeXl,
    lineHeight: text.lineTight,
    color: colors.text,
  },
  subtitle: {
    margin: 0,
    fontSize: text.sizeXs,
    lineHeight: text.lineMeta,
    color: colors.textMuted,
  },
  board: {
    display: "flex",
    flexDirection: "column",
    gap: space.md,
  },
  /** One policy: its name, its bar, its numbers. Clicking it opens the policy. */
  scoreRow: {
    display: "grid",
    gridTemplateColumns: "minmax(8rem, 14rem) minmax(0, 1fr) auto",
    alignItems: "center",
    gap: space.lg,
    width: "100%",
    paddingInline: space.md,
    paddingBlock: space.md,
    textAlign: "left",
    cursor: "pointer",
    borderWidth: "1px",
    borderStyle: "solid",
    borderColor: { default: colors.line, ":hover": colors.lineStrong },
    backgroundColor: { default: "transparent", ":hover": colors.glass },
    outline: { default: "none", ":focus-visible": `2px solid ${colors.ring}` },
    outlineOffset: { default: null, ":focus-visible": "-2px" },
  },
  scoreRowSelected: {
    borderColor: colors.accent,
    backgroundColor: colors.accentSoft,
  },
  policy: {
    display: "flex",
    flexDirection: "column",
    gap: space.xxs,
    minWidth: 0,
  },
  policyId: {
    overflow: "hidden",
    textOverflow: "ellipsis",
    whiteSpace: "nowrap",
    fontSize: text.sizeSm,
    fontWeight: text.weightMedium,
    color: colors.text,
  },
  policyMeta: {
    fontSize: text.sizeMicro,
    lineHeight: text.lineMicro,
    color: colors.textMuted,
  },
  bars: {
    display: "flex",
    flexDirection: "column",
    gap: space.xs,
    minWidth: 0,
  },
  barTrack: {
    position: "relative",
    height: "0.5rem",
    width: "100%",
    borderRadius: radii.sm,
    backgroundColor: colors.chip,
  },
  barFillScore: {
    position: "absolute",
    insetBlock: 0,
    left: 0,
    borderRadius: radii.sm,
    backgroundColor: colors.accent,
  },
  barFillRoute: {
    position: "absolute",
    insetBlock: 0,
    left: 0,
    borderRadius: radii.sm,
    backgroundColor: colors.signalGreen,
  },
  barLegend: {
    display: "flex",
    gap: space.lg,
    fontFamily: text.fontMono,
    fontSize: text.sizeMicro,
    lineHeight: text.lineMicro,
    color: colors.textMuted,
  },
  numbers: {
    display: "flex",
    flexDirection: "column",
    alignItems: "flex-end",
    gap: space.xxs,
    fontFamily: text.fontMono,
    fontSize: text.sizeXs,
    color: colors.text,
  },
  versionLink: {
    fontFamily: text.fontMono,
    fontSize: text.sizeMicro,
    color: colors.textMuted,
    backgroundColor: "transparent",
    borderWidth: 0,
    padding: 0,
    cursor: "pointer",
    textDecorationLine: { default: "none", ":hover": "underline" },
  },
});
