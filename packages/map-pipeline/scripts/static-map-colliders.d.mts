/**
 * Types of the static-collider producer this subpath re-exports
 * (`@simforge-oss/maps/ingest`). This file used to carry its own copy of the
 * declarations, which drifted from the producer (a wrong schema string, no
 * vertical extents); it now declares nothing of its own.
 */
export * from '@simforge-oss/maps/ingest';
