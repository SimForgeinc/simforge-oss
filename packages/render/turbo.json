{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "test": {
      "dependsOn": ["transit", "build:artifacts", "^build:artifacts", "build"],
      "outputs": []
    }
  }
}
