import * as stylex from "@stylexjs/stylex";
import { colors, motion, space, stroke, text } from "../../stylex/tokens.stylex";

export const styles = stylex.create({
  root: {
    display: "flex",
    flexDirection: "column",
    minHeight: 0,
    height: "100%",
    width: "100%",
  },
  /** Stays put while the list scrolls: the section's name, its count, its one primary action. */
  header: {
    display: "flex",
    flexDirection: "column",
    gap: space.s2,
    paddingInline: space.s3,
    paddingBlock: space.s3,
    borderBottomWidth: stroke.hairline,
    borderBottomStyle: "solid",
    borderColor: colors.hairline,
  },
  headerRow: {
    display: "flex",
    alignItems: "center",
    justifyContent: "space-between",
    gap: space.s2,
  },
  title: {
    margin: 0,
    color: colors.mutedForeground,
  },
  count: {
    fontFamily: text.fontMono,
    fontSize: text.sizeXs,
    color: colors.mutedForeground,
  },
  scroller: {
    minHeight: 0,
    flex: "1 1 0%",
    overflowY: "auto",
    overflowX: "hidden",
    paddingBottom: space.s3,
  },
  group: {
    display: "flex",
    flexDirection: "column",
  },
  groupHeader: {
    position: "sticky",
    top: 0,
    zIndex: 1,
    display: "flex",
    alignItems: "baseline",
    justifyContent: "space-between",
    gap: space.s2,
    paddingInline: space.s3,
    paddingBlock: space.s2,
    backgroundColor: colors.bg,
    color: colors.mutedForeground,
  },
  groupTitle: {
    minWidth: 0,
  },
  list: {
    listStyle: "none",
    margin: 0,
    padding: 0,
    display: "flex",
    flexDirection: "column",
  },
  row: {
    display: "flex",
    width: "100%",
    alignItems: "flex-start",
    gap: space.s2,
    borderWidth: 0,
    borderLeftWidth: stroke.thick,
    borderLeftStyle: "solid",
    borderLeftColor: "transparent",
    paddingInline: space.s3,
    paddingBlock: space.s2,
    textAlign: "left",
    cursor: "pointer",
    backgroundColor: { default: "transparent", ":hover": colors.fillSubtle },
    outline: { default: "none", ":focus-visible": `2px solid ${colors.ring}` },
    outlineOffset: { default: null, ":focus-visible": "-2px" },
    transitionProperty: "background-color, border-color",
    transitionDuration: motion.durFast,
    transitionTimingFunction: motion.easeStandard,
  },
  rowSelected: {
    backgroundColor: colors.fill,
    borderLeftColor: colors.accent,
  },
  rowBody: {
    display: "flex",
    minWidth: 0,
    flex: "1 1 0%",
    flexDirection: "column",
    gap: space.s0_5,
  },
  rowTitle: {
    minWidth: 0,
    fontSize: text.sizeSm,
    fontWeight: text.weightMedium,
    color: colors.text,
  },
  rowMeta: {
    display: "flex",
    flexWrap: "wrap",
    alignItems: "center",
    gap: space.s1_5,
    fontSize: text.sizeXs,
    lineHeight: text.lineXs,
    color: colors.mutedForeground,
  },
  rowTrailing: {
    display: "flex",
    flexShrink: 0,
    alignItems: "center",
    gap: space.s1,
  },
  /** Policies under their campaign: indented, quieter, still selectable rows. */
  subList: {
    listStyle: "none",
    margin: 0,
    padding: 0,
    display: "flex",
    flexDirection: "column",
    paddingLeft: space.s6,
  },
  empty: {
    paddingInline: space.s3,
    paddingBlock: space.s4,
  },
  /** The section-level statement: quieter than a row, louder than nothing. */
  status: {
    paddingInline: space.s3,
    paddingBlock: space.s2,
    borderBottomWidth: stroke.hairline,
    borderBottomStyle: "solid",
    borderColor: colors.hairline,
  },
});
