import * as stylex from "@stylexjs/stylex";
export const styles = stylex.create({ root: { borderTopWidth: 1, borderBottomWidth: 1, borderColor: "rgb(255 255 255 / 10%)", paddingBlock: "1rem", color: "white" } });
